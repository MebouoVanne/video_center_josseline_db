{% extends 'base.html.twig' %}
{% block body %}
	<div class="row">
		<div class="col-md-6 mx-auto">
			<h1 class="text-center my-5">Mettre à jour le profil</h1>
			{{ form_start(userForm) }}
			{{ form_row(userForm.firstname)}}
			{{ form_row(userForm.lastname)}}
			{{ form_row(userForm.email)}}
			{{ form_row(userForm.imageFile)}}
			<button type="submit" class="btn btn-primary mt-4">Appliquer les modifications
			</button>
			{{ form_end(userForm) }}
		</div>
	</div>
{% endblock %}
